I think I have other projects with projects and applications that I'm building right now, which would all go hand in hand together, actually. I tried to explain it structurally. I think this transcription feature is something that should be done, which is great. The user just has to acknowledge that he has downloaded such a big file, and it's a feature that is only on demand and not used often. I can imagine, so that's fine and should be done. For the images, I don't think we need to roll everything of that on our own, because I already have a distinct project which is called PickPetite. It is the sister project. If you go a folder above and search for PickPetite, there is a project which is already targeting to be a privacy-first and client-side image compressor service, which also offers an API that allows to compress images. The only thing to decide here right now is whether NoteFade should use the API of PickPetite, or if NoteFade should copy over the logic and code of PickPetite and then re-implement it, so that no API call is necessary. An API call again has many downsides, and it needs to have the other website/web application available. Also, it might not be as private as I would like it to be, and it can change. Maybe copying over just the logic of how PickPetite already handles image compressing might be best, and then I would not root for webp but for avif actually, because the compression is even more aggressive there and we can allow better limits and bigger images. Something needs to be explored and thought through about the decision. For videos, there's another project which is called VoidHop. If you go one project above or one directory above this current NoteFade project, you will find a project called VoidHop. VoidHop is a privacy-first client-side link shortener service, and this would allow maybe the video feature, because the video feature could be changed for NoteFade in a way that the user knows that he can not use the big chunked multi or chain multi storage URL directly. What we could do is we could still create such a very, very, very giant URL, which might even break the URL length standard, but it is only for this video case. This is a special case. We have this enormous URL which chains many, many, many, many, many chunks, like we raise the chunk limit and we chain up way more chunks, URLs, and chunks than usually. We use the privacy-first URL shortener and provide the user a new shortened URL. We can rely on the fact that the URL shortener service is already privacy-first and client-side only and just stores chunked key-value pairs as well that are encrypted. Take a look into this project, and you will see that this project already features what we need here. It allows to privacy-first safely store a very big amount of data or URL and just have a very short link in return, a shortened link. This might be the missing edge that we need to also implement video chat, and if everything and every connection is made well throughout all the different services, the original claims of privacy and security are still intact.
